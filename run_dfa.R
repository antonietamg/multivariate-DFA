################## RUN #################################

#define data and run function

x <- your_data
dplot <- dfa_function(x, pol, sec) #x = dataframe; pol = polynomial; sec = scale values (for scale, use get_scale function)
write(alpha_val, file = "alphadfa")
